short_description: >
 Measuring MIPS during 7z compression and decompresssion on each thread then get the avarage MIPS.

description: |
 Measuring MIPS during compression and decompresssion on each thread then get the avarage MIPS. 
 Note: Million instructions per second (MIPS) is an approximate measure of a computer's raw
 processing power.

homepage: https://www.7-zip.org/

parameters:
 
results:
 openssl-basic.Compression.MIPS: MIPS
 openssl-basic.Decompressiong.MIPS: MIPS